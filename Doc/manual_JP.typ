//文章フォーマット設定
#set text(font: "Times New Roman", size: 11pt)
#show regex("[\u3000-\u9fa5]"): set text(font: "Yu Gothic", lang: "ja")
#set page(paper: "a4", margin: (x: 2cm, y: 2cm))
#set math.equation(numbering: numbering.with("(1)"), supplement: "式")
#set heading(numbering: "1.")
//外部パッケージ
#import "@preview/physica:0.9.3": *

#align(center)[
  #text(size: 18pt, weight: "bold")[pyrpa マニュアル]
  #v(0.5em)
  #text(size: 11pt)[2025年]
]

#v(1em)

= pyrpaについて

pyrpaは, モデルハミルトニアン（タイトバインディング模型）を入力として, 様々な物性量を計算・可視化するPythonコードです。主にWannier関数に基づいた第一原理計算の結果をポスト処理する用途を想定しています。

現在サポートしている主な計算機能は以下の通りです。

- バンド構造・フェルミ面の計算と可視化
- 状態密度（DOS）の計算
- スペクトル関数 $A(bold(k), omega)$ の計算
- ボルツマン理論・線形応答理論による各種伝導係数（電気伝導率, 熱伝導率, ゼーベック係数など）の計算
- RPA（乱雑位相近似）に基づくスピン感受率 $chi_s$ の計算
- FLEX（揺らぎ交換近似）による自己エネルギーの計算
- 線形Eliashberg方程式による超伝導固有値・ギャップ関数の計算

pyrpaはすべての設定を`main.py`の上部にある変数を書き換えることで行います。ライブラリのimport行より上にある変数のみを変更することを想定しています。

= 動作環境

以下のPythonパッケージが必要です。

- `numpy`
- `scipy`
- `matplotlib`
- `skimage`（option=17のサイクロトロン質量計算で使用）

Fortranで書かれた内部ライブラリ（`libs/flibs`, `libs/plibs`）がコンパイルされている必要があります。

= 入力ファイルについて

=== ファイル形式の選択（`ftype`）

ハミルトニアンの入力ファイル形式を`ftype`変数で指定します。`fname`にファイル名またはディレクトリ名（拡張子なし）を文字列で代入してください。

#table(
  columns: (auto, 1fr),
  [*ftype*], [*ファイル形式*],
  [0], [`{fname}/` ディレクトリ内に `ham_r.txt`, `irvec.txt`, `ndegen.txt` が存在する形式],
  [1], [`{fname}.input` ファイル（独自形式）],
  [2], [`{fname}_hr.dat` ファイル（Wannier90のデフォルトホッピングファイル）。*第一原理計算との連携では通常これを使用*],
  [3], [MLO（最局在ワニエ）基底の非直交基底ホッピング形式],
  [その他], [`Hopping.dat` ファイル（ecaljのホッピングファイル）],
)

*Wannier90を使用する場合の例：*

```python
fname = 'inputs/Sr2RuO4'   # _hr.dat を除いたパスを指定
ftype = 2
```

=== スピン軌道相互作用（`sw_soc`）

`sw_soc = True` にすることでスピン軌道相互作用（SOC）を考慮した計算を行います。SOCを含む系では, ハミルトニアンがスピン自由度を含む（軌道数が2倍になる）形式である必要があります。現在, option=12,13のFLEX/Eliashberg計算でのSOC対応は実装中です。

= ブラベー格子の設定（`brav`）

`brav`変数でブラベー格子の種類を指定します。これは逆格子ベクトルの計算やsymmetry lineの自動生成に使用されます。量子ESPRESSO（QE）やWannier90の出力と対応する設定が用意されています。

#table(
  columns: (auto, 1fr),
  [*brav*], [*格子の種類（対応するコード）*],
  [0], [単純格子（Simple cubic / tetragonal / orthorhombic）],
  [1], [面心立方格子（QEデフォルト, ibrav=2 相当）],
  [2], [体心立方格子（QEデフォルト, ibrav=3 相当）],
  [3], [六方晶格子（Hexagonal）],
  [4], [三方晶格子（Trigonal, QEの sbrav=5 相当）],
  [5], [底心格子（Base-centered）],
  [6], [面心立方格子（慣例的な向き）],
  [7], [体心立方格子（慣例的な向き）],
  [その他], [単斜晶格子（Monoclinic）],
)

= 計算モード（`option`）

計算内容は`option`変数の整数値で指定します。各モードの詳細を以下に示します。

=== option=0: バンドプロット

symmetry lineに沿ったバンド分散 $E_n(bold(k))$ を計算し描画します。対称線は`k_sets`と`xlabel`で指定するか, 指定がなければ`brav`の設定から自動で選びます。

=== option=1: 状態密度（DOS）計算

$ "DOS"(omega) = - 1/pi sum_(bold(k), n) "Im" G^0_n (bold(k), omega + i delta) $

全軌道の合計と軌道ごとの部分DOSをプロットします。

=== option=2: 2次元フェルミ面のプロット

指定した$k_z$断面（デフォルト: $k_z = 0$, 変数`kz`で変更可能）における$k_x$-$k_y$面のフェルミ面を描画します。電子数`fill`またはフェルミエネルギーから化学ポテンシャルを自己無撞着に求め, $E_n(bold(k)) = mu$ となる等エネルギー面を描きます。`RotMat`で回転行列を指定することでフェルミ面を回転させることもできます。

=== option=3: 3次元フェルミ面のプロット

Marching cubesアルゴリズムを用いて3次元フェルミ面をポリゴンで描画します。`kscale`で各軸方向の表示スケールを変えることができます。

=== option=4: スペクトル関数のプロット

一体グリーン関数のトレースの虚部から電子のスペクトル関数

$ A(bold(k), omega) = -1/pi "Im" "Tr" G(bold(k), omega + i delta) $

を計算し, symmetry lineに沿って描画します。`sw_self=True`の場合はFLEX計算で求めた自己エネルギーを取り込み, 相互作用の効果を含めたスペクトルを計算します。

=== option=5: ボルツマン理論による伝導係数

ボルツマン輸送方程式を用いて以下の各種輸送係数を計算します。緩和時間近似（$tau = $ `tau_const` fs）を使用します。

出力される物理量：
- 電気伝導率テンソル $sigma_{i j}$ （単位: S/m）
- 熱伝導率テンソル $kappa_{i j}$ （単位: W/m/K）
- ゼーベック係数テンソル $S_{i j}$ （単位: V/K）
- パワーファクター $sigma S^2$ （単位: $upright(W \/ m \/K^2)$）
- ペルチェ係数, ローレンツ数

=== option=6: 線形応答理論による伝導係数

久保公式（Kubo formula）に基づく線形応答理論を用いて電気伝導率などを計算します。`delta`パラメータが実効的な緩和時間 $tau approx hbar / delta$ に対応します。

=== option=7: スピン感受率スペクトルのプロット

RPAによるスピン感受率

$ chi_s (bold(q), omega) = frac(chi^0 (bold(q), omega), 1 - S chi^0 (bold(q), omega)) $

をsymmetry lineに沿って計算し, 虚部のスペクトルをプロットします（パラマグノンスペクトル）。相互作用行列$S$はオンサイト相互作用`U`, `J`から生成されます。結果は`chis.dat`に出力されます。

=== option=8: 特定$bold(q)$点でのスピン感受率

`at_point`で指定した波数$bold(q)$点における$chi_s (bold(q), omega)$の周波数依存性を計算・プロットします。

=== option=9: 特定エネルギーでの$bold(q)$空間スピン感受率マップ

`Ecut`で指定したエネルギー$omega_0$における$chi_s (bold(q), omega_0)$の$k_x$-$k_y$空間分布をプロットします。ネスティングベクトルの可視化などに使用します。

=== option=10: 超伝導感受率スペクトルのプロット

超伝導（ペアリング）感受率 $phi(bold(q), omega)$ をsymmetry lineに沿って計算しプロットします。

=== option=11: 特定エネルギーでの$bold(q)$空間超伝導感受率マップ

`Ecut`で指定したエネルギーにおける超伝導感受率の$bold(q)$空間分布をプロットします。

=== option=12: FLEX（揺らぎ交換近似）による自己エネルギー計算

FLEXアルゴリズムを自己無撞着に解き, 電子の自己エネルギー $Sigma(bold(k), i omega_n)$ を松原周波数空間で計算します。得られた自己エネルギーは`self_en.npz`に保存されます。

- `sw_out_self=True`: 自己エネルギーをファイルへ出力
- `sw_in_self=True`: 前回の自己エネルギーを初期値として読み込む（反復計算の続きを行う）

=== option=13: 線形Eliashberg方程式の解

RPAまたはFLEXで求めた有効ペアリング相互作用を使い, 線形化されたEliashberg方程式を固有値問題として解きます。最大固有値$lambda$と対応するギャップ関数$Delta(bold(k), i omega_n)$が得られます。$lambda = 1$で超伝導転移温度$T_c$となります。

- `sw_self=False`: 自己エネルギー補正なし（RPA）
- `sw_self=True`: FLEXの自己エネルギーを用いる（`self_en.npz`が必要）
- `sw_from_file=True`: `self_en.npz`からファイルを読み込む
- `gap_sym`: 初期ギャップ関数の対称性を指定（詳細は後述）
- ギャップ関数は`gap_{ij}.dat`に出力される

=== option=14: ギャップ関数の後処理

option=13で計算したギャップ関数（`gap.npy`）を読み込み, 以下の量を計算・出力します。

- ギャップ関数の異常グリーン関数 $F(bold(k), i omega_n)$
- Padé近似による実周波数へ解析接続したスペクトル

=== option=15: キャリア数の計算

各バンドのフェルミ面の体積から電子数（粒子数）・ホール数を計算します。`fill`との整合性確認などに使います。

= カラープロット設定（`color_option`）

option=0,2,3では, バンドやフェルミ面の各点に色をつけることができます。

#table(
  columns: (auto, 1fr),
  [*color_option*], [*意味*],
  [0], [色なし（黒単色）],
  [1], [`olist`で指定した軌道成分をRGB（赤/緑/青）の混合比で表示],
  [2], [群速度 $|bold(v)(bold(k))|$ の大きさを色で表示],
)

`color_option=1`の場合, `olist`で軌道インデックスを `[R成分, G成分, B成分]` の形で指定します。複数の軌道を同じ色に割り当てる場合は, リストの要素をさらにリストにします。例:

```python
olist = [[0, 3], [1, 4], [2, 5]]
# 軌道0と3をRed, 軌道1と4をGreen, 軌道2と5をBlueに対応
```

= 各種パラメータの詳細説明

以下では, `main.py`の上部に記載されている全パラメータについて物理的な意味を含めて説明します。

=== 基本設定

- `fname` （文字列）: 入力ファイルのパス（`ftype`に応じた形式で指定, 拡張子は通常不要）

- `ftype` （整数）: 入力ハミルトニアンのファイル形式（3節参照）

- `brav` （整数）: ブラベー格子の種類（4節参照）

- `sw_soc` （bool）: スピン軌道相互作用を考慮するかどうかのスイッチ

=== メッシュ設定

- `Nx, Ny, Nz` （整数）: 第一ブリルアンゾーン内の$bold(k)$点メッシュ数。フェルミ面積分など3次元の$bold(k)$積分で使用します。精度を上げるには大きく（例: 32×32×32）, メモリ・時間と要相談です。

- `Nw` （整数）: 松原周波数のメッシュ数（option=12,13のFLEX/Eliashberg計算）または実周波数のメッシュ数（option=1,4など）。温度$T$に対して$omega_n = (2n+1)\pi T$の形で$n=0,1,...,N_w-1$まで取ります。

- `kmesh` （整数）: バンド・スペクトルをsymmetry lineに沿って計算する際の$bold(k)$点数。大きいほど滑らかなプロットが得られます（例: 200～500程度）。

=== 格子定数

- `abc` （リスト, 単位: Å）: 格子定数$a, b, c$。速度 $v = 1/hbar (partial E)/( partial bold(k))$ の計算やsymmetry lineの幅の計算（実空間の長さ換算）に使用します。Wannier90の場合はWIN/WOUTファイルの値に合わせてください。

- `alpha_beta_gamma` （リスト, 単位: degree）: 格子角度$alpha, beta, gamma$。直方晶・立方晶の場合は `[90., 90., 90.]` です。

=== 温度・化学ポテンシャル

- `tempK` （実数, 単位: K）: 温度をケルビンで指定します。コード内で $T = k_B$`tempK`（単位: eV）に変換されます。

- `temp` （実数, 単位: eV）: $k_B T$を直接eV単位で指定する場合に使います。`tempK`と同時に定義した場合は`temp`が優先されます。

- `fill` （実数）: バンドフィリング（1格子点あたりの電子数）。例えば3バンドモデルで3電子なら`fill=3.0`。化学ポテンシャル$mu$の計算に使用します。

- `mu0` （実数, 単位: eV）: 化学ポテンシャルを直接指定したい場合に定義します。定義されている場合, `fill`からの自己無撞着計算をスキップして`mu0`の値を使用します。

=== エネルギー範囲・収束パラメータ

- `Emin, Emax` （実数, 単位: eV）: DOSやスペクトルを計算する際のエネルギー範囲の下限・上限。

- `delta` （実数, 単位: eV）: スペクトル計算のブロードニングパラメータ $delta$。グリーン関数の虚部に加える小量で, ディラクのデルタ関数を有限幅のローレンツ型に広げます。大きすぎると特徴が潰れ, 小さすぎると数値ノイズが出ます（0.01〜0.05 eV程度が目安）。

- `Ecut` （実数, 単位: eV）: option=9,11で$bold(q)$空間の感受率マップを作成する際の固定エネルギー$omega_0$（フェルミ面近傍は0付近に設定）。

=== 伝導計算パラメータ

- `tau_const` （実数, 単位: fs）: ボルツマン理論（option=5）での定数緩和時間$tau$。実際の系では散乱機構に依存しますが, 定数近似では実験値との比較から決めます（例: 金属では1〜100 fs程度）。

- `sw_tdf` （bool）: `True`にすると輸送分布関数（TDF）を先に計算し, そのエネルギー積分から輸送係数を求めます（エネルギー依存緩和時間に対応する場合に有効）。

=== 軌道・相互作用パラメータ

- `olist` （リスト）: カラープロット（color_option=1）で使用する軌道インデックスのリスト（6節参照）。

- `U` （実数, 単位: eV）: オンサイトクーロン斥力（ハバード$U$）。FLEX/RPA計算で使用。磁性・超伝導の計算には重要なパラメータです。

- `J` （実数, 単位: eV）: オンサイトフント結合定数。$U' = U - 2J$（金森遮蔽）が自動で計算されます。

- `orb_dep` （bool）: `True`にすると軌道依存の相互作用行列`Umat`, `Jmat`を使用します。`False`（デフォルト）では`U`, `J`の一定値を全軌道に適用します。

=== ギャップ関数の対称性（`gap_sym`）

Eliashberg方程式（option=13）を解く際の初期ギャップ関数の対称性を指定します。

#table(
  columns: (auto, 1fr),
  [*gap_sym*], [*対称性*],
  [0], [$s$波（全ての$bold(k)$で正）],
  [1], [$d_{x^2-y^2}$波（$cos k_x - cos k_y$型）],
  [2], [$s^plus.minus$波（ネスティングベクトルを挟んで符号反転）],
  [3], [$d_{x y}$波（$sin k_x sin k_y$型）],
  [-1], [$p_x$波],
  [-2], [$p_y$波],
)

=== $bold(k)$空間の設定

- `kz` （実数, 規格化単位）: option=2のフェルミ面プロットにおける$k_z$の値。$0 \sim 0.5$の範囲で指定します（$k_z = 0$は$Gamma$点面, $k_z = 0.5$はゾーン境界面）。

- `kscale` （リスト or 実数）: option=3の3次元フェルミ面表示における各軸の表示スケール。例: `kscale=[1.0, 1.0, 0.5]`とすると$k_z$方向を半分に縮めて表示。

- `k_sets` （リストのリスト）: symmetry lineの各端点のk座標（規格化単位, $0 \sim 1$）。自動生成ではなく手動で対称線を設定したい場合に定義します。例: `k_sets=[[0,0,0],[0.5,0,0],[0.5,0.5,0]]`

- `xlabel` （文字列のリスト）: `k_sets`に対応するラベル。例: `xlabel=[r'$\Gamma$','X','M']`

- `at_point` （リスト）: option=8で感受率を計算する$bold(q)$点の座標（規格化単位）。

=== スイッチ類

- `sw_unit` （bool）: `True`（デフォルト）で国際単位系（SI）の物理定数を使用し, 出力も実際の物理単位で得られます。`False`にすると$hbar = k_upright(B) = e = 1$の無次元系になります。

- `sw_omega` （bool）: option=11でスピン感受率を実周波数（`True`）または松原周波数（`False`）で計算するかのスイッチ。

- `sw_self` （bool）: option=13のEliashberg計算でFLEXの自己エネルギーを使うかどうか。`True`にすると繰り込まれたグリーン関数を用います。

- `sw_out_self` （bool）: `True`にするとFLEX計算の自己エネルギーを`self_en.npz`に出力します。

- `sw_in_self` （bool）: `True`にするとFLEX計算の初期自己エネルギーとして前回の結果を読み込みます（自己無撞着ループの収束を早める）。

- `sw_from_file` （bool）: `True`にすると自己エネルギーを`self_en.npz`から読み込み, FLEX計算を実行せずに直接Eliashberg計算を行います。

- `sw_dec_axis` （bool）: True にすると格子ベクトルを適切に分解して逆格子ベクトルを設定します。

= 典型的な計算フローの例

=== フェルミ面とバンドの確認

まずバンド構造とフェルミ面を確認します。

```python
fname, ftype, brav, sw_soc = 'inputs/SomeMaterial', 2, 0, False
option = 0          # バンドプロット
fill = 2.0          # 電子数
abc = [4.0, 4.0, 4.0]
alpha_beta_gamma = [90., 90., 90.]
tempK = 300
```

次にフェルミ面を確認します。

```python
option = 2          # 2次元フェルミ面
color_option = 1    # 軌道成分を色で表示
olist = [0, 1, 2]   # 各軌道をR/G/Bに対応
```

=== 超伝導ギャップ関数の計算（RPA法）

```python
option = 13         # 線形Eliashberg方程式
Nx, Ny, Nz, Nw = 32, 32, 1, 512
tempK = 50          # やや低温で計算
fill = 2.0
U, J = 1.0, 0.1
gap_sym = 1         # d_{x^2-y^2}波で初期化
sw_self = False     # RPAで計算（FLEX自己エネルギーなし）
sw_out_self = True  # ギャップ関数を出力
```

計算が終わると標準出力に固有値$lambda$が表示され, `gap_{ij}.dat`にギャップ関数が出力されます。

=== FLEX + Eliashberg計算

自己エネルギーを繰り込んだより精密な計算の場合は, まずoption=12でFLEX計算を行い, 次にoption=13を`sw_self=True`, `sw_from_file=True`で実行します。

```python
# ステップ1: FLEX自己エネルギー計算
option = 12
sw_out_self = True

# ステップ2: Eliashberg方程式（FLEX自己エネルギー込み）
option = 13
sw_self = True
sw_from_file = True
```

= トラブルシューティング

- *化学ポテンシャルが収束しない*: `Nx, Ny, Nz`を増やすか, `tempK`を少し上げてください。

- *スペクトルにノイズが多い*: `delta`を少し大きくする, または`Nw`を増やしてください。

- *バンドがおかしい*: `brav`の設定が合っているか確認してください。特にQEとの連携では`brav=1`（FCC）や`brav=2`（BCC）のconventionに注意が必要です。

- *Eliashberg計算が収束しない*: `U`を小さくする, `tempK`を高くする, メッシュ`Nx,Ny,Nz`を増やすなどを試してください。固有値$lambda$が1を超えている温度は超伝導相内に対応します。

- *伝導率の単位が合わない*: `sw_unit=True`になっているか確認してください。`False`にすると無次元系になります。
